# 更新记录 (Changelog)

## v5.0 (2026-08-14) —— 🏁 最终开源版

> ⚠️ **最终开源版声明**
>
> 本版本是 AdPurge 的**最终开源版**，作者本人**后续不再更新**。
>
> - **为什么不留个人信息**：就是怕你们找作者反馈问题、让作者改这改那。代码和文档都在这里，有问题请自行解决。
> - **作者的初心**：把这个项目分享出来，是希望你们能自己动手、做得更好，而不是你们提要求让作者来实现。
> - **免责**：部分新增功能未经过大量测试，如有问题请自行解决。

### 📦 版本号变更

- 版本号由 v3.1 直接升至 **v5.0**（versionCode 23 → 50），v3.1 与 v3.1.1 的全部内容并入本版；部分性能与设置类内容归档至 v4.0 记录
- 关于页面**去除 QQ / 邮箱联系方式，只保留作者名**（与最终开源版「不留联系方式」口径一致）

### 🔑 正式发布签名

- 构建签名由 debug 证书更换为正式发布证书（RSA-2048 / CN=AdPurge）
- 修复旧版签名查看工具报「Archive is not a ZIP archive」问题：补齐 v1 (JAR) 签名（v1+v2+v3 三重签名全开），`jarsigner -verify` 显示 `jar verified.`
- 固化 `sign-release.sh` 发布签名脚本（v1/v2/v3 三重签名一键完成）

### 🚀 核心功能（三批 16 项方案落地）

- **批量处理多选**：文件选择支持多选 APK，逐个自动处理（自动全选厂商、独立工作目录、完成后汇总对话框+通知）
- **断点续传闭环**：单文件处理用固定工作目录，解包/DEX 修补阶段落检查点；处理失败/中断后再次选择同一 APK 自动弹「断点恢复」对话框，跳过已完成的解包与 DEX 修补直接打包签名，失败中间产物保留可重试
- **一键去除签名校验**：一键去广告选项新增「杀签」开关；处理前自动预判 DEX 含签名校验时弹三态警告（取消 / 仍要处理 / 去签并处理）。判定双条件防误伤：方法名含 signature/checksig/verifysig 等词根 **且** 方法体实际引用签名校验特征字符串或调用 signature/SigningInfo 类型
- **在线特征库更新**：设置-广告特征配置新增「在线更新特征库」按钮，输入 URL 下载 JSON 特征库并与当前配置按分类合并去重，不覆盖已有条目
- **处理完成通知**：处理后发送系统通知（Android 13+ 运行时请求通知权限），可在设置中关闭
- **处理期间前台服务防杀**：点击开始处理后启动前台服务（ProcessingService 低优先级常驻通知、不可划掉、START_NOT_STICKY），切后台回微信不会被系统杀进程；用户主动划掉任务卡/厂商强杀则由断点续传兜底恢复
- **处理报告增强**：报告新增「缩短开屏倒计时 / 去除签名校验」统计行；报告文件名带时间戳，多次处理不覆盖
- **真实进度条**：进度条由空转动画改为真实进度（按 DEX 修补进度 0~100% 推进）
- **签名密钥自持**：签名改为应用随机生成 keystore + 随机密码（v1+v2+v3 三重签名，兼容全部 Android 版本），支持密钥导出/导入迁移（p12、jks/.keystore 双格式），不再依赖测试密钥；应用设置底部「签名密钥」卡片——密钥信息（别名/格式/证书 SHA256 指纹）展示、导出密钥+密码说明文件、导入密钥（SAF 选择器，失败弹密码框重试）
- **签名密钥 JKS/.keystore 导入支持**：自动识别 JKS（FEEDFEED 魔数）与 PKCS12；JKS 支持库密码与条目密码分开输入（Android Studio 生成的默认两者相同）；导入前先在源文件验证成功才落盘，导入后沿用原条目别名签名；导出按当前格式生成 `AdPurge-signing.p12` 或 `AdPurge-signing.jks` + 密码说明文件

### 🧬 广告特征库合并升级（并入原作者最新版）

- 特征库 12 类 716 条 → **18 类 1160 条**：并入原作者新增 6 类（ad_asset_paths 资产路径 / lib_file_keywords SO库名 / asset_keywords 资产文件名 / method_neutralize_keywords 方法中性化 / root_file_keywords 根目录文件 / res_layout_keywords 布局文件名），保留本应用独有的 ad_key_strings 与权限全名
- assets 清理升级：路径前缀 + 文件名关键词 + 根目录残留三类特征接入
- 新增广告 .so 原生库清理：按 lib_file_keywords 删除广告 so 文件（loadLibrary 已在 DEX 阶段 NOP，无闪退风险）
- method_neutralize_keywords / res_layout_keywords 两类**先入库暂不执行**（误伤面大，待验证原作者语义后自行接入）

---

## v4.0 (2026-08-14)

> 从 v3.1 划分归档的性能与设置类内容。

### ⚡ 性能提升

- **双线程 DEX 并行**：内存充足时（≥700MB）两个 DEX 并发修补、批间串行控峰值内存，DEX 修补阶段再提速；低内存设备自动回退单线程
- **丢弃调试信息可选化**：默认开启（DEX 更小、写回更快），可在设置中关闭以保留崩溃日志行号
- 真机实测（51.8MB / 9 DEX 测试 APK，12 家厂商全开，512MB 堆设备走单线程路径）：**DEX 修补 48.1s（v3.0 为 51.2s，快 6%）、总耗时 65.2s（v3.0 为 62.3s，增量来自新增的杀签预扫描/报告增强等环节）、处理后 49.9MB**。杀签 5 个方法置恒通过、开屏倒计时缩短 4763 处、assets 清理 4 个，全部生效。双线程并行需 ≥700MB 堆设备触发，提速幅度待高端设备复测

### ⚙️ 设置与体验

- **设置项开关**：设置-处理选项新增 5 个开关：双线程并行 / 丢弃调试信息 / 缩短开屏倒计时 / 完成通知 / 默认杀签
- **处理报告开关**：设置-处理选项新增「生成处理报告」开关（默认开启），关闭后处理完成不再保存 Markdown 报告
- 更新检查地址改为占位仓库（`YOUR_GITHUB_NAME/AdPurge`），发布前替换为真实仓库即可生效
- 界面语言框架就绪（AppConfig 语言键），内建深色主题保持

---

## v3.0.1 (2026-08-13)

> Bug 修复 + 功能对齐版：修复「勾选 VPN 去除 / 虚拟机检测后处理的包闪退」问题，并采纳同类工具（二改作者 0813 版）实测稳定的两个功能。

### 🐛 修复

- **修复勾选 VPN 去除 / 虚拟机检测后处理的包闪退**：根因是检测方法仅按方法名包含匹配（contains），业务方法只要名字带 vpn/emulator 字样就被置为 return false，导致调用方初始化/分支逻辑断裂。现对齐同类工具实测稳定方案——**方法体证据检查**：方法体内指令必须实际引用网络接口、系统属性、模拟器特征路径等检测证据字符串才判定为检测方法，且仅处理 boolean 返回类型的方法，不再误伤业务方法

### ✨ 新增功能（采纳同类工具实测稳定特性）

- **开屏广告倒计时缩短**：类名含 splash 的类中，追踪常量写入寄存器的值，命中 `Handler.postDelayed` / `sendEmptyMessageDelayed` / `sendMessageDelayed` / `CountDownTimer` 且延迟 ≥1000ms 时把延迟常量置 0，开屏广告倒计时立即结束、直接进入主界面（同类工具实测单 APK 306~483 处命中）
- **签名校验扫描提示**：16 个签名校验特征关键词扫描全部 DEX，处理前若检测到签名校验逻辑即弹出警告，提示「处理后重新签名可能导致应用校验失败闪退」，避免用户误认为是去广告修补本身的问题，用户可选择取消或继续处理

---

## v3.0 (2026-08-13)

> 性能攻坚版：以同类工具（二改作者 0813 版）为基准逐项实测对比，深挖出「debug 构建性能降级」「dexlib2 懒解析重复兑现」「debug_info 状态机编码」三大隐藏瓶颈并逐一击破。
> 同一 51.8MB / 9 DEX 测试 APK 实测（12 家厂商全开）：**总耗时 161.3s → 62.3s（快 2.6 倍），比同类工具（82.9s）快 25%**。

### ⚡ 性能提升（v3.0 核心亮点）

- **总耗时 161.3s → 62.3s**：DEX 修补 147.3s → 51.2s，打包签名 9.1s → 6.9s，处理后 APK 从 50.7MB 缩至 49.5MB
- **Release 构建**：debuggable 应用 ART 执行性能降级（解释执行为主），改 Release 构建后整体快 2 倍以上
- **方法级深拷贝预解析（关键优化）**：dexlib2 的 dex-backed 对象是懒解析的，写盘时 DexWriter 遍历全部指令多遍（字符串池/类型池/编码），每遍都重新解析字节流 → N 遍遍历 = N 次全量解析 + GC 风暴。提前在扫描期把方法解析成 Immutable 对象，写盘时零解析：单 DEX 写回由 8~19.5s 降至 2.9~7.1s
- **丢弃 debug_info**：写回时不再重编码调试信息（行号表/局部变量表的状态机编码），写回更快且 DEX 体积缩小 16%（未压缩 106.1MB → 89.8MB）；不影响 APK 运行，仅处理后 APK 的崩溃日志不显示源码行号
- **写盘前释放原始 DEX 树**：扫描完成后释放 dex-backed 对象树，写盘前堆占用由 362MB 降至 200MB 以内，大 APK 不再有内存不足风险
- **方法级零拷贝**：被修改类中未修改的方法直接复用原始引用，仅重建被修改的方法
- **取消资源引用二次全量加载**：assets 资源引用在 DEX 修补遍历时顺手收集，无需修补完成后再把全部 DEX 重新加载扫描一遍
- **每个 DEX 处理后无条件 GC**：下一个 DEX 的加载与写回在干净的堆上进行

---

## v2.0 (2026-08-13)

> 基于 v1.7 版本的全面升级版，历经 4 轮迭代（v1.8 ~ v1.11）深度优化。
> 本版本重点解决「处理卡死、内存爆炸、体积膨胀」三大问题，并大幅提升处理速度与使用体验。
> 三次开发（v1.8 ~ v2.0）：消失の阿力

### ⚡ 性能提升（核心亮点）

- **DEX 写回速度大幅提升（对标同类工具实测）**：通过「Release 构建」「方法级深拷贝预解析」「写盘前释放原始 DEX 树」「每个 DEX 处理后无条件 GC」四项深度优化，同一 51.8MB / 9 DEX 测试 APK 实测：**DEX 修补 147.3s → 53.9s（快 2.7 倍），总耗时 161.3s → 64.7s，比同类工具（82.9s）快 22%**
- **方法级深拷贝预解析（关键优化）**：dexlib2 的 dex-backed 对象是懒解析的，写盘时 DexWriter 遍历全部指令多遍（字符串池/类型池/编码），每遍都重新解析字节流 → N 遍遍历 = N 次全量解析+GC 风暴。提前在扫描期把方法解析成 Immutable 对象，写盘时零解析：单 DEX 写回由 8~19.5s 降至 2.9~7.7s
- **方法级零拷贝**：被修改类中未修改的方法直接复用原始引用，仅重建被修改的方法
- **写盘前释放原始 DEX 树**：扫描完成后释放 dex-backed 对象树，写盘前堆占用由 362MB 降至 200MB 以内，大 APK 不再有内存不足风险
- **取消资源引用二次全量加载**：assets 资源引用在 DEX 修补遍历时顺手收集，无需修补完成后再把全部 DEX 重新加载扫描一遍
- **处理速度提升 41%**：同一 52.2MB / 10 DEX APK 实测，总耗时由 326.9 秒降至 192.4 秒；其中扫描与修改阶段由 130 秒降至 2 秒以内
- **彻底根治处理卡死**：此前处理到第 3 个 DEX 时界面会冻结数秒（处理最终能完成但全程卡顿），现全程流畅无冻结
- **彻底根治内存爆炸**：单 DEX 处理内存峰值由约 150MB 降至 30~40MB；10 个 DEX 连续处理内存平稳，不再出现内存不足崩溃
- **厂商识别提速约 10 倍**：识别阶段由「按厂商逐个重复扫描全部 DEX」改为「一次扫描完成全部识别」，识别结果与优化前完全一致
- **无广告 DEX 自动跳过**：不含所选厂商特征的 DEX 自动跳过，不处理不写回；勾选的厂商越少、跳过的 DEX 越多、处理越快
- **零修改 DEX 跳过写回**：实际无任何修改的 DEX 直接保留原样，省去最耗时的写回步骤
- **识别结果复用，杜绝重复扫描**：厂商识别阶段的结果直接复用于处理阶段判定，无需重复读取分析
- **处理日志大幅精简**：由 2 万多行逐类明细精简为每个 DEX 仅 2~3 行进度汇总，日志清晰直观、界面刷新更流畅

### 🚀 新增功能

- **处理报告自动生成**（Markdown 格式）：厂商识别清单、每个 DEX 的修改统计、处理前后体积与耗时对比，与处理后 APK 同目录保存
- **处理日志一键复制**：日志面板一键复制全部内容到剪贴板，方便分享与反馈问题
- **断点续传基础设施**：处理流程分阶段记录检查点，为后续断点恢复功能打下基础
- **大文件安全扫描**：超大 DEX 扫描内存占用由 50MB+ 降至约 2MB，低内存设备也能稳定处理大型 APK
- **DEX 写入安全保护**：写入前自动备份、异常中断自动恢复原文件，杜绝处理中途崩溃导致 DEX 损坏

### 🐛 Bug 修复

- 修复处理后 APK 体积翻倍的问题（处理后体积恢复与原始 APK 持平）
- 修复 Android 11+ 设备安装处理后 APK 报错 -124 的问题（资源表压缩与对齐问题，处理后 APK 可正常安装）
- 修复广告 SDK 厂商误识别的问题（短关键词在 DEX 数据中的误匹配）
- 修复关闭厂商开关后仍被处理的问题（开关现在精确生效，未勾选的厂商完全保留）
- 修复小屏手机「开始处理」按钮不显示的问题（任意分辨率下按钮始终可见）
- 修复处理报告显示「处理后 0B」的问题（体积对比数据完整显示）
- 修复报告 DEX 顺序混乱的问题（classes10 错误排在 classes2 前，现按 classes → classes2 → ... 自然顺序排列）
- 修复大 DEX 处理内存不足崩溃的风险

### 📝 其他改进

- 处理报告格式由 HTML 升级为 Markdown，更易阅读与分享
- 处理日志全面精简优化：删除逐类明细，保留 DEX 级进度与完成汇总（含耗时）
- 处理统计口径修正：「修改类数」等数据现在准确反映实际修改结果

---

## 历史版本

（v1.7 及更早版本记录不再列出）